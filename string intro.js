let string="how are you";
console.log(string);
let c= string.charAt(4);
let ascii=string.charCodeAt(4);//returns ascii value of index elemnet
let substr=string.substring(2,8);
let split=atring.split("o")//splits the string accoring to the paramenter inside quotes
let join=string.join("+")// joins all words using the element in quotes
